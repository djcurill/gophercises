- We need to build a sitemap generator.
- Given an html URL, we should fetch the URL.
- This URL should be provided as a flag
- We should record the domain of the url. We do not want to parse links to other websites.
- Given the output of the webpage, parse the html for links.
- Scan the links and check (1) They belong to the sitemap host and (2) Only collect new links
- Because we need to check if a link has already been visited
-

